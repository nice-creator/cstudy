数组的定义
    a、 arr和[]结合是数组
    b、 将确定的元素个数放入[]中
    c、 用元素的类型定义一个普通变量
    d、 从上往下整体替换
如 : int arr[10];

注意：
    1 数组名 arr(也可以是其他的)不能和其他变量名同名
    2 数组的元素下标从0开始
    3 访问数组的时候注意不要越界
    4 数组的元素等价于普通变量
    5 定义数组的时候，[]里边不能是变量

遍历数组
int arr=[10];
int i=0;
for(i=0;i<10;i++)
    {
        printf("%d ",arr[i]); //输出都是不确定的值，定义数组的时候元素没有初始化
    }





